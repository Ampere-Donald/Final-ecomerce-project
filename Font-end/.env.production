# Build de production : l'API est appelée en relatif (même domaine que le
# site) et le Worker Cloudflare (worker.js) proxifie /api et /uploads vers
# le backend Railway. Ne pas mettre d'URL absolue ici — les anciens Android
# ne reconnaissent pas le certificat Let's Encrypt de Railway.
VITE_API_URL=/api
